package com.brawlstars.ConferenceService.repo;

import com.brawlstars.ConferenceService.models.NumberCount;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NumberCountRepository extends JpaRepository<NumberCount, Long> {}
